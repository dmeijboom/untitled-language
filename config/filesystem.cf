exports {
    type Filesystem: object {
        uuid: string
        path: string
        fstype: string
        opts: string?
    }

    let filesystems: []Filesystem

    let fs: Filesystem = new {
        uuid = "hello"
        path = "/"
        fstype = "btrfs"
    }
}

main {
    let name: string = "Dillen Meijboom"
    
    writeln(name)
}
