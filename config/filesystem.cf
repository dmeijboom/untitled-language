exports {
    type Filesystem: object {
        uuid: string
        path: string
        fstype: string
        opts: string?
    }

    let filesystems: []Filesystem

    let fs: Filesystem = new {
        uuid = "hello"
        path = "/"
        fstype = "btrfs"
    }

    let fs_uuid: string = "Hello World"
    
    writeln(fs_uuid)
}
