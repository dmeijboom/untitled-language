type Filesystem: object {
    uuid: string
    path: string
    fstype: string
    opts: string?
}

let filesystems: []Filesystem

let fs: Filesystem = new {
    uuid = "hello"
    path = "/"
    fstype = "btrfs"
}

filesystems.add(fs)
